name: build

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: go vet ./...
      - run: go test -race ./...

  desktop:
    name: desktop (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        # ubuntu-22.04: webview_go links webkit2gtk-4.0, which 24.04 dropped
        # in favor of 4.1.
        os: [ubuntu-22.04, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Install webview dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev
      - name: Build desktop shell
        run: go build -tags desktop -trimpath -ldflags "-s -w" -v ./cmd/alexander
      - name: Stage artifact
        shell: bash
        run: |
          mkdir -p dist
          if [ "${{ runner.os }}" = "Windows" ]; then
            mv alexander.exe dist/
          else
            mv alexander dist/
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: alexander-${{ matrix.os }}
          path: dist/

  ebitengine:
    name: ebitengine (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        # Pure Go, no system dependencies: any runner works.
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Build native client
        run: go build -trimpath -ldflags "-s -w" -v ./cmd/alexander-ebitengine
      - name: Stage artifact
        shell: bash
        run: |
          mkdir -p dist
          if [ "${{ runner.os }}" = "Windows" ]; then
            mv alexander-ebitengine.exe dist/
          else
            mv alexander-ebitengine dist/
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: alexander-ebitengine-${{ matrix.os }}
          path: dist/

  wasm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Build WASM client
        run: GOOS=js GOARCH=wasm go build -trimpath -ldflags "-s -w" -o alexander.wasm ./cmd/alexander-ebitengine
      - name: Stage artifact
        run: |
          mkdir -p dist
          mv alexander.wasm dist/
          cp "$(go env GOROOT)/lib/wasm/wasm_exec.js" dist/
      - uses: actions/upload-artifact@v4
        with:
          name: alexander-wasm
          path: dist/
